@page
@using fisketorvet_project_v1.Models
@model fisketorvet_project_v1.Pages.AdminSection.CustomerAdminPageModel
@{
}

<h1>Customer Management Page</h1>

<div class="back" style="background-color: rgba(255, 255, 255,0.75)">
    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Email</th>
                <th>Address</th>
                <th>Phone Number</th>
                <th>UserName</th>
                <th>is Admin?</th>
                <th>
                    <a asp-page="AddProduct2Store"
                       class="btn btn-primary btn-group-lg">
                        Add New
                    </a>
                </th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Customers.Count != 0)
            {
                @foreach (Customer customer in Model.Customers.Values)
                {
                    <tr>
                        <td>
                            <b>@customer.Id</b>
                        </td>
                        <td>
                            <b>@customer.Name</b>
                        </td>
                        <td>
                            <b>@customer.Email</b>
                        </td>
                        <td>
                            <b>@customer.Address</b>
                        </td>
                        <td>
                            <b>@customer.PhoneNumber</b>
                        </td>
                        <td>
                            <b>@customer.UserName</b>
                        </td>

                        <td>
                            <b>@customer.Admin</b>
                        </td>
                        <td>
                            <a asp-page="EditProduct"
                               class="btn btn-success btn-group-lg pull-right">
                                Edit
                            </a>
                            <form method="post">
                                <input align="right" type="submit" class="btn btn-danger" value="Remove" style="background-color: red; color: black" asp-route-id="@customer.Id" asp-page="/Delete/DeleteCustomer" />
                            </form>
                        </td>
                    </tr>

                }
            }
        </tbody>
    </table>
</div>